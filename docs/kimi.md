 审查完成。先给结论，再分页细说。

   复盘页：核心问题是"三套分数并存 + 卡片平铺"

   当前报告区有 11+ 张卡片等权重纵向堆叠，数据散在 4 处且口径不一：

   1. 分数重复展示 — ReviewPage.tsx:558 的特色 3 分、:612 的五维聚合分、:699

   的客观指标、:663 的题目清单，四处都是"成绩"，用户不知道哪个算数。

   2. 行动区重复 — "最优先改这一点"(:576）与"下一轮练习"(:840）展示同一个 issue、同一个

   /retry/:id CTA，一张卡能说完的事用了两张。

   3. 真实交互 bug — 五维评测的 &lt;details open={item.key ===

   lowestDimensionKey}&gt;(:629）是受控写法，用户手动展开某维度后，任何 state

   变化（如点"查看逐字稿"）触发 re-render 都会把它折回去。需改用 useState 记录展开项。

   4. 客观指标单元格过载 — 5 列 × 每格 3 行（标签/值+徽章/小字说明）,:707 的 lg 5

   列布局下非常挤。

   建议按"金字塔"收敛为 4 层：

   ┌──────────────┬─────────────────────────────────────┬──────────────────────────────┐

   │ 层           │ 内容                                │ 动作                         │

   ├──────────────┼─────────────────────────────────────┼──────────────────────────────┤

   │ 1 成绩条     │ 综合分/特色分 + 语速·填充词精简指标 │ 合并现有 featuredScores      │

   │              │                                     │ 与客观指标卡                 │

   ├──────────────┼─────────────────────────────────────┼──────────────────────────────┤

   │ 2 下一步     │ 主问题 → 证据 → 成功标准 → 唯一复练 │ 合并"最优先"与"下一轮练习"两 │

   │              │ CTA                                 │ 卡                           │

   ├──────────────┼─────────────────────────────────────┼──────────────────────────────┤

   │ 3 诊断明细   │ 五维手风琴（taskChecks              │ 修复 details 受控 bug        │

   │              │ 并入"场景任务"维度），默认只展开最  │                              │

   │              │ 低分                                │                              │

   ├──────────────┼─────────────────────────────────────┼──────────────────────────────┤

   │ 4            │ 改写对比、整篇逻辑、逐字稿、录音    │ 保持折叠，层次不变           │

   │ 证据与原始材 │                                     │                              │

   │ 料           │                                     │                              │

   └──────────────┴─────────────────────────────────────┴──────────────────────────────┘

   其他页面

   - 首页：模式选择在右列、"开始练习"按钮在左列 hero

   卡，选完模式要跨列找按钮。建议模式卡底部加 CTA，或点 ModeCard 直接跳练习页。

   - 历史页：每条记录右侧徽章槽位混用三种语义（"复练有进步"/"填充词

   N"/"无指标",HistoryPage.tsx:281-287)，建议拆成固定两列；时间格式加上年份。

   - 画像页:"当前特点"卡与"下一步训练"卡重复展示

   focus.title/suggestion(ProfilePage.tsx:200-210 vs :219-227)，可合并为一卡。

   - 练习页：顶部徽章一行最多 6 个（模式/时长/录音/轮次/ASR/LLM)，建议 ASR、LLM 状态并入下方

    GuidancePanel（已有模型检查项），顶部只留会话状态。

   - 布局/复练页：结构健康，无需改动。

   要我按这个方案动手改复盘页吗？还是先从某一处（比如 details bug 或行动区合并）开始？